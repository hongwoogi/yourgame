# Local-only database. Never point development tests at the production database.
TURSO_DATABASE_URL=file:.local/development.db
TURSO_AUTH_TOKEN=
APP_ORIGIN=http://localhost:3000

# Load the operator-provided Google web client into .env.local, never public/.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
